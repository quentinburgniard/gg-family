<%= form_with(model: travel_companion_appearance, local: true) do |form| %>
  <% if travel_companion_appearance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(travel_companion_appearance.errors.count, "error") %> prohibited this travel_companion_appearance from being saved:</h2>

      <ul>
      <% travel_companion_appearance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :travel_companion_appearance_name %>
  </div>

  <div class="field">
    <%= form.label :profile_picture %>
    <%= form.text_field :profile_picture, id: :travel_companion_appearance_profile_picture %>
  </div>

  <div class="field">
    <%= form.label :welcome_picture %>
    <%= form.text_field :welcome_picture, id: :travel_companion_appearance_welcome_picture %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
